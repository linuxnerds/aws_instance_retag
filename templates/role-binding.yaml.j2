apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: "{{ aws_instance_retag_service_account }}"
  namespace: "{{ aws_instance_retag_namespace }}"
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: cluster-admin
subjects:
- kind: ServiceAccount
  name: "{{ aws_instance_retag_service_account }}"
  namespace: "{{ aws_instance_retag_namespace }}"
